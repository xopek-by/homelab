services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      DOMAIN: "${DOMAIN}"
    volumes:
      - ${PATH_TO_DATA}:/data
    ports:
      - 8000:80